/**
 * Product constants. The plan catalog itself comes from the API
 * (GET /api/plans); these values are shared marketing/config facts.
 */
export const SERVICE_NAME = "Network Access";
export const DEVICE_LIMIT = 5;
